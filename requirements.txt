playwright>=1.45,<2.0
python-dotenv>=1.0,<2.0
anthropic>=0.34,<1.0
# HTTP client for the optional TypeSafe Jev decider (jev.py, DECIDER=hybrid).
# Already installed as a dependency of anthropic; listed because jev.py uses it directly.
httpx>=0.27,<1.0
openpyxl>=3.1,<4.0
discord.py>=2.7,<3.0
# Optional: only needed for the voice front-end (python voice.py). Speech-to-
# text runs locally; the first run downloads the Whisper model once.
# faster-whisper
# sounddevice
# pyttsx3
# Optional: only needed if you set LLM_PROVIDER=openai, deepseek, gemini or openrouter in .env
# openai>=1.40,<2.0
# Optional: only needed if you turn on ANY of the ENABLE_MCP_* flags in
# .env (fetch, Brave Search, or filesystem -- see mcp_tools.py) -- pinned
# below v2 since the MCP Python SDK's v2 release rewrote its server/client
# API; the reference "fetch" server (and this project's mcp_tools.py) are
# both built against the well-established v1 API.
# mcp<2
# Optional: only needed if you set ENABLE_MCP_FETCH=true specifically --
# this is the fetch server itself, a separate package from the `mcp`
# client library above.
# mcp-server-fetch
# ENABLE_MCP_BRAVE_SEARCH and ENABLE_MCP_FILESYSTEM need no extra pip
# package -- their servers are launched via `npx` (Node.js), not pip. You
# do need Node.js/npm installed on your machine for either of those two.
# Optional: only needed if you set ENABLE_WINDOWS_AUTOMATION=true --
# Windows-only. Drives apps via UI Automation (a real accessibility tree),
# the same reliability class as Playwright reading the DOM, not blind
# pixel/coordinate clicking.
# pywinauto
